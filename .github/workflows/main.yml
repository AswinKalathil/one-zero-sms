name: CI Test Pipeline

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Run tests
        run: |
          echo "Running tests..."
          
